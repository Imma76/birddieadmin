import 'package:flutter/material.dart';

class DATES extends StatelessWidget {
  // final String title;
  const DATES({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: const Text('DATES'));
  }
}
